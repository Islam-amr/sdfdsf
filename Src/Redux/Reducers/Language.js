import * as ActionTypes from '../Actions/ActionTypes'

const initialState = {
    RTL: true
}

export const Language = (state = initialState, action) => {

    switch (action.type) {
        default:
            return state;
    }
};

